// datatype --> represntation of data, operation on data

// representation of integer
// 2 bytes --> 16 bti --> 1 bit for sign and 15 bit for storing number
// we can apply arithemetic operations
// we can use ( pre / post )increment or decrement
// pre --> first increment/decrement then perform
// post --> first perform then increment/decrement

// abstract means hidden --> internal data is abstract from us
// Abstract datatype --> define the data, operations on data together and used as datatype,
// by hiding all the internal details